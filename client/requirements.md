## Packages
framer-motion | Page transitions and list staggering animations for premium feel
lucide-react | Icons for the UI

## Notes
The public bio links page uses `dir="rtl"` for Arabic layout.
Forms in the admin panel expect URL strings for images (avatarUrl, imageUrl).
Backend is expected to provide /api/profile and /api/links endpoints.
